import { styled } from "@mui/material";

const MainDiv = styled("div")({
  height: "100vh",
  textAlign: "center",
});

const Body = () => {
  return (
    <div>
      <MainDiv>BODY</MainDiv>
    </div>
  );
};

export default Body;
